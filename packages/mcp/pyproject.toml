[project]
name = "suitest-mcp"
version = "0.1.0"
description = "Suitest MCP client + registry (stub for M0; full impl in M1c)"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/suitest_mcp"]
