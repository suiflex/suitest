"""Test-case generators (M2).

Deterministic, ZERO-tier-first generators plus the rule-based target classifier
they consult. LLM-driven generators are layered on top in later tasks. No
re-exports here (barrel files are banned) — import submodules directly.
"""
