[project]
name = "suitest-core"
version = "0.1.0"
description = "Suitest core capability primitives"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/suitest_core"]
